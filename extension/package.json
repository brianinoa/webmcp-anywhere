{
  "name": "@webmcp-anywhere/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WebMCP Anywhere — MV3 Chrome extension that registers WebMCP tools on every website.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@webmcp-anywhere/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "jsdom": "^26.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.3.0",
    "vitest": "^3.1.0"
  }
}
